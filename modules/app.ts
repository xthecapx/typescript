//import { PI, calculateCircunference } from "./math/circle";
import * as Circle from "./math/circle";
import calc from "./math/rectangle"

console.log(Circle.PI, Circle.calculateCircunference(10));
console.log(calc(10, 20));